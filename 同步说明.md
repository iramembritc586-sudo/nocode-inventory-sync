# 手机 / PC 实时同步说明

本版已加入“手机 / PC 实时同步”。

## 使用方式

1. 手机端和电脑端打开同一个网址。
2. 顶部会显示“同步批次号”，默认是当天日期。
3. 手机端点击“复制PC链接”，把链接发到电脑打开；或者电脑端手动填写同一个同步批次号。
4. 手机端每保存一条数据后，会自动写入云端。
5. 电脑端约 3 秒内自动拉取一次同批次数据。
6. 点击“查看清单”可以看到当前同步到的记录。

## 注意

- 这里是准实时同步：保存后约 3 秒内同步，不是每按一个数字都同步。
- 不同盘点日期/批次可以用不同的同步批次号，避免混在一起。
- 导出时会合并本机数据和云端同批次数据。
